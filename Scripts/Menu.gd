extends Control



#func _ready():
	#for interface in IP.get_local_interfaces():
		#if "en1" in interface.name: #If interface is wifi
			#print(interface.addresses[1])
	#print(IP.get_local_addresses())
